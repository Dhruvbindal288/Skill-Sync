import User from "../models/user.model.js";


export const updateSkills=async(req,res)=>{
    const userId = req.user.id;
let {want_to_learn,want_to_teach}=req.body

}

export const deleteSkills=async(req,res)=>{
    const userid=req.user.id;
}